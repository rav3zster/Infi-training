import type { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'com.trainingtracker.app',
  appName: 'Training Tracker',
  webDir: 'dist'
};

export default config;
